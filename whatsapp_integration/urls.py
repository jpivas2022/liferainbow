# =============================================================================
# WHATSAPP INTEGRATION - URLs
# =============================================================================

from django.urls import path

app_name = 'whatsapp_integration'

urlpatterns = [
    # Webhook URLs são definidas na API principal
    # Este arquivo existe para compatibilidade com include()
]
